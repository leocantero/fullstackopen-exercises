``` mermaid

sequenceDiagram
    participant user
    participant browser
    participant server

    user ->> browser: saves a note

    browser ->> server: POST /new_note
    activate server
    server ->> browser: HTTP 302 redirect to /notes
    deactivate server

    browser ->> server: GET /notes
    activate server
    server ->> browser: notes.html
    deactivate server

    Note right of browser: browser starts requesting css and js files

    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/main.css
    activate server
    server ->> browser: main.css
    deactivate server

    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/main.js
    activate server
    server ->> browser: main.js
    deactivate server

    Note right of browser: browser starts executing js code and requesting data.json

    browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
    activate server
    server ->> browser: data.json
    deactivate server
```