``` mermaid
sequenceDiagram
    participant user
    participant browser
    participant server

    user ->> browser: saves new note

    browser ->> server: POST /new_note
    activate server
    server ->> browser: 201 Created
    deactivate server

    Note right of browser: browser execute js and renders the new note

```