``` mermaid
sequenceDiagram
    participant browser
    participant server

    browser ->> server: GET /spa
    activate server
    server ->> browser: spa.html
    deactivate server

    Note right of browser: browser starts requesting css and js files

    browser ->> server: GET /main.css
    activate server
    server ->> browser: main.css
    deactivate server

    browser ->> server: GET /spa.js
    activate server
    server ->> browser: spa.js
    deactivate server

    Note right of browser: browser execute js and requests notes (AJAX)

    browser ->> server: GET /data.json
    activate server
    server ->> browser: data.json
    deactivate server

```